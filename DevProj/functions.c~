//functions to build linked lists

  struct opcode 
{
    char *name;
    int value;
    struct opcode *next;
}start;
  
  struct opcode* createOpcodeList(char *name,int codeValue);
  struct opcode* addOpcode(char* name, int codeValue);



  
  struct symbolTab
{
    char *Symname;
    int Symvalue;
    struct symbolTab *next;
}symbol;

  struct symbol* creatSymbol(char *Symname, int Symvalue);
  struct symbol* addOp(char *Symname, int Symvalue);



    


  


  
